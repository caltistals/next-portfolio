import React from "react";
import Link from "next/link";
import { siteConfig } from "../site.config";

const Navbar = () => {
  return (
    <nav className="fixed top-0 bottom-0 w-10 overflow-y-auto bg-slate-300">
      <ul>
        <li>d</li>
      </ul>
    </nav>
  );
};

export default Navbar;
